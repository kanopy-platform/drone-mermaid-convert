# drone-mermaid-convert

Reads a `drone.yml` and outputs mermaid markdown state diagram with the entire visual representation of the pipeline.

Usage
```
drone-mermaid-convert <path to drone file> > output.md
```
